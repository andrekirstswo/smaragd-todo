query GetBoard($boardId: String!) {
  board(boardId: $boardId) {
    boardId
    name
    sections {
      boardSectionId
      name
      order
    }
    accesses {
      userId
      role
    }
  }
}